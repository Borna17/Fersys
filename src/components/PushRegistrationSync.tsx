export default function PushRegistrationSync() {
  // Native push auto-registration is temporarily disabled while Firebase
  // Android configuration is being verified. Keeping this component as a
  // no-op preserves router structure and prevents startup-time plugin errors.
  return null
}
