name: Apply server recovery snapshots

on:
  push:
    branches:
      - fix/local-first-durability-engine
    paths:
      - scripts/apply-server-recovery-snapshots.mjs
      - .github/workflows/apply-server-recovery-snapshots.yml
  workflow_dispatch:

permissions:
  contents: write

jobs:
  patch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: fix/local-first-durability-engine
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Integrate recovery snapshots
        run: node scripts/apply-server-recovery-snapshots.mjs
      - run: npm ci
      - run: npm run build
      - run: git diff --check
      - name: Commit verified recovery integration
        run: |
          if git diff --quiet; then
            echo "No patch changes to commit"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/services/drafts.service.ts
          git commit -m "Persist synced drafts to recovery history"
          git push origin HEAD:fix/local-first-durability-engine
