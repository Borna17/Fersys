name: Validate FERSYS polish 2026-09-13

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/validate-polish-20260913.yml'

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - run: npm run build

# Validation marker: intern time tracking release candidate.
